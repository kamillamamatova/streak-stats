name: Update GitHub stats

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:
  push:
    branches:
      - master
      - main

permissions:
  contents: write

concurrency:
  group: update-github-stats
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate stats SVG
        run: npm run generate
        env:
          STATS_USERNAME: kamillamamatova
          STATS_TOKEN: ${{ secrets.STATS_TOKEN || github.token }}
          STATS_ACCENT: "#ec4899"
          STATS_ACCENT_SOFT: "#fbcfe8"
          STATS_BG: "#fff5fb"

      - name: Commit updated stats
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update GitHub streak stats"
          file_pattern: assets/github-streak-stats.svg
